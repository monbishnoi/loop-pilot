{
  "name": "looppilot",
  "version": "0.1.0",
  "description": "Orchestration for autonomous agent loops.",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "bin": {
    "looppilot": "dist/src/cli/index.js"
  },
  "files": [
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.test.js",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "keywords": [
    "agents",
    "trajectory",
    "tool-use",
    "knn",
    "mcp",
    "agent-harness"
  ],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.1",
    "express": "^5.1.0",
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.5",
    "@types/node": "^25.0.0",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
